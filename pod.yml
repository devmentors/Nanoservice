apiVersion: v1
kind: Pod
metadata:
  name: nanoservice-pod
  labels:
    name: nanoservice
spec:
  containers:
  - name: nanoservice
    image: nanoservice
    imagePullPolicy: Never
    ports:
    - containerPort: 5000